# 生产环境示例 —— 真实值由 GitHub Actions Secrets 注入，不要把真实 URL 提交到这个文件
# 在 GitHub 仓库 Settings → Secrets and variables → Actions 添加：
#   VITE_SERVER_URL = wss://twinum-server.fly.dev
VITE_SERVER_URL=wss://twinum-server.fly.dev
